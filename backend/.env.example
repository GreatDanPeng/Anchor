# Get a key at https://openrouter.ai/keys — this key is billed only for
# whatever isn't served by the free-model pool (which should be $0 in normal use).
OPENROUTER_API_KEY=sk-or-...

# Optional
PORT=8787
